export default function Hero({ children, home }) {
    return (
        <div class="hero">

            <div class="hero-content">
                {children}
            </div>

        </div>
    )
}